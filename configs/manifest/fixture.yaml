run_id: fixture-contract-v1
output_root: runs
artifact_root: artifacts/demo
seed: 20260421

fixture:
  image_size: 96
  lesions_per_split: 6
  stable_pairs_per_split: 4
  changing_pairs_per_split: 4
  source_dataset: synthetic_fixture

preprocessing:
  profile: fixture_224_center_crop_v1
  image_size: 224

metrics:
  bootstrap_samples: 300
  ci_level: 0.95
  fixed_tpr: 0.80
